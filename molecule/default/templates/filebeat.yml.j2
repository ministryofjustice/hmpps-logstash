filebeat:
  inputs:
    - type: log
      enabled: true
      paths:
        - /var/log/nginx/*.log
      
output:
  logstash:
    hosts: ["localhost:5044"]

multiline:
  pattern: '^\[[0-9]{4}-[0-9]{2}-[0-9]{2}'
  negate: true
  match: after
